module.exports = {
    mediaDir: "devmedia", // Root folder for photos
    metaDir: "devmetadata", // Folder to store the database into
    serverPort: 3000, // Port that the server listens to
    defaultResponsePageSize: 50 // Default page size for fetching photos from the database
}
